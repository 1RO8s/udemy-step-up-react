import styled from "styled-components";

export const Users = () => {
  return (
    <SContainer>
      <h2>User list</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
